# 사용하지 않는 렌더러 (v2 잔재)

`pipeline.py` 가 유일한 렌더러입니다. 이 폴더의 세 파일은 v2 시절의
`templates/` + `render.py` 방식이며, `pipeline.py` 와 **결과물이 다릅니다.**

두 개의 렌더러가 나란히 있으면 "어느 쪽으로 뽑은 이미지인가"를 알 수 없게 되고,
그건 이 프로젝트에서 가장 비싼 종류의 혼선입니다. 그래서 실행 경로에서 치웠습니다.
삭제하지 않은 이유는 안전 영역·폰트 크기 계산에 참고할 값이 남아 있어서입니다.
참고를 마치면 지우세요.
