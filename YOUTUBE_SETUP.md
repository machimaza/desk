# 유튜브 자동 업로드 열쇠 만들기

막히기 쉬운 자리가 세 군데 있습니다. 먼저 말씀드립니다.

| 함정 | 무슨 일이 생기나 | 피하는 법 |
|---|---|---|
| **동의 화면이 '테스트' 상태** | 갱신 토큰이 **7일마다 만료**됩니다 | 4단계에서 **프로덕션으로 게시** |
| **앱이 검수 전** | 영상이 **비공개로만** 올라갑니다 | 업로드는 하고, 공개는 손으로. 검수는 따로 신청 |
| **두 번째 동의부터 토큰이 안 옴** | `refresh_token` 이 응답에 없습니다 | `prompt=consent` 를 붙입니다 (스크립트가 이미 붙입니다) |

---

## 1. 프로젝트 만들기

<https://console.cloud.google.com/projectcreate>

- 프로젝트 이름 `machimaza`
- 위치는 그대로

## 2. API 켜기

<https://console.cloud.google.com/apis/library/youtube.googleapis.com>

**사용** 버튼을 누릅니다. 프로젝트가 `machimaza` 인지 위쪽에서 확인하세요.

## 3. 동의 화면 만들기

**API 및 서비스 → OAuth 동의 화면**

| 칸 | 넣을 값 |
|---|---|
| User Type | **외부** |
| 앱 이름 | `마치마자 업로더` |
| 사용자 지원 이메일 | 본인 메일 |
| 개발자 연락처 | 본인 메일 |

**범위** 단계에서 **범위 추가 또는 삭제** → 아래를 찾아 체크합니다.

```
https://www.googleapis.com/auth/youtube.upload
```

목록에 안 보이면 아래 칸에 직접 붙여넣고 **테이블에 추가**를 누르세요.

## 4. 프로덕션으로 게시 ← 여기가 중요합니다

동의 화면 요약 페이지에 **앱 게시** 버튼이 있습니다. 누르세요.

**테스트 상태로 두면 갱신 토큰이 7일마다 만료됩니다.** 그러면 일주일마다
토큰을 다시 만들어야 합니다. 게시하면 만료되지 않습니다.

게시해도 구글 검수를 받은 것은 아닙니다. 동의할 때 "확인되지 않은 앱" 경고가
뜨는데, **고급 → 계속** 을 누르면 됩니다. 본인 계정에만 쓰는 것이라 문제없습니다.

## 5. 클라이언트 ID 만들기

**API 및 서비스 → 사용자 인증 정보 → 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**

| 칸 | 넣을 값 |
|---|---|
| 애플리케이션 유형 | **데스크톱 앱** |
| 이름 | `machimaza-uploader` |

**데스크톱 앱**을 고르는 이유는 리디렉션 주소를 안 물어보기 때문입니다.
웹 애플리케이션을 고르면 주소를 정확히 맞춰야 해서 여기서 많이 막힙니다.

만들면 **클라이언트 ID** 와 **클라이언트 보안 비밀번호**가 나옵니다. 둘 다 복사해 두세요.

## 6. 갱신 토큰 받기

본인 컴퓨터에서 아래를 돌립니다. **시크릿은 이 컴퓨터 밖으로 안 나갑니다**
(구글에게만 보냅니다).

```
cd $HOME\Desktop\machimaza
git pull
python scripts/yt_token.py
```

1. 클라이언트 ID 와 시크릿을 붙여넣습니다
2. 브라우저가 열립니다 → 유튜브 계정(`machimaza`)으로 동의
3. "확인되지 않은 앱" 경고 → **고급 → 계속**
4. **연결할 수 없는 페이지**로 넘어갑니다. **정상입니다**
5. 그 페이지의 **주소창을 통째로 복사**해서 붙여넣습니다
6. `YT_REFRESH_TOKEN` 값이 출력됩니다

## 7. Secrets 에 넣기

<https://github.com/machimaza/desk/settings/secrets/actions>

| Name | 값 |
|---|---|
| `YT_CLIENT_ID` | 5단계의 클라이언트 ID |
| `YT_CLIENT_SECRET` | 5단계의 보안 비밀번호 |
| `YT_REFRESH_TOKEN` | 6단계에서 나온 값 |

---

## 올리기

Actions → **발행** → Run workflow

| 칸 | 값 |
|---|---|
| `target` | `youtube` |
| `privacy` | `private` (검수 전에는 이것만 됩니다) |
| `publish` | 처음에는 **false** 로 두고 미리보기 |

미리보기에서 제목·설명을 확인하고, 그다음에 `publish` 를 켜세요.

올라간 영상은 유튜브 스튜디오에 **비공개**로 들어옵니다. 확인한 뒤
스튜디오에서 공개로 바꾸시면 됩니다.

## 쇼츠로 잡히려면

세 조건이 다 맞아야 합니다. 우리 영상은 이미 다 맞습니다.

- 세로 9:16 (1080×1920) ✅
- 60초 이하 (지금 47·52초) ✅
- 제목이나 설명에 `#Shorts` ✅ 스크립트가 제목에 붙입니다

## 하루 몇 개까지

업로드는 하루 100회까지 됩니다. 하루 한 편 올려도 한참 남습니다.

## 검수를 신청하려면

공개로 바로 올리고 싶으시면 동의 화면에서 **인증 준비** 를 눌러 신청합니다.
사용 목적과 영상(스크린캐스트)을 내야 하고 2~4주 걸립니다.

급하지 않다면 **비공개로 올리고 손으로 공개 전환**하는 편이 낫습니다.
어차피 발행 전에 사람이 확인할 항목(이미지 저작권·과장 뉘앙스·YMYL)이 있어서,
스튜디오에서 한 번 보고 공개하는 흐름과 잘 맞습니다.
