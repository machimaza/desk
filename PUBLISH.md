# 발행 절차 — 브라우저 자동화

API 경로는 막혀 있습니다. 티스토리 Open API 는 2024년 2월 종료(글쓰기 포함),
네이버 블로그는 개인 블로그용 공개 글쓰기 API 가 없습니다.
**남은 길은 브라우저뿐입니다.** 그래서 이 문서가 필요합니다.

---

## 원칙 — 임시저장까지만 자동, 발행 버튼은 사람이

| | 자동 | 사람 |
|---|---|---|
| 로그인 | ✗ | ✓ 미리 로그인된 브라우저를 씁니다 |
| 제목·본문·태그 입력 | ✓ | |
| 이미지 업로드 | ✓ | |
| 임시저장 | ✓ | |
| **발행** | ✗ | **✓ 반드시 사람이 누릅니다** |

**왜 발행까지 자동화하지 않는가**

1. **네이버는 자동화 탐지가 있습니다.** 계정 제재는 되돌릴 수 없고,
   이 프로젝트에서 계정은 자산 그 자체입니다. 글 한 편 아끼자고 걸 판돈이 아닙니다.
2. 발행은 되돌리기 어렵습니다. 잘못된 수치가 색인되면 그때부터 우리도
   "틀린 숫자를 쓰는 블로그" 중 하나가 됩니다. 마지막 사람 눈이 필요합니다.
3. 임시저장까지만 해도 귀찮은 일의 90%가 사라집니다. 남는 건 확인과 클릭뿐입니다.

---

## 준비 — 브라우저를 몰기 전에

```bash
python3 scripts/verify.py       content/<폴더>   # 게이트부터 통과해야 합니다
python3 scripts/build_publish.py content/<폴더>   # publish/ 생성
```

`publish/` 안에 이렇게 생깁니다.

| 파일 | 쓰는 곳 |
|---|---|
| `tistory.html` | 티스토리 **HTML 모드**에 통째로 붙여 넣기 |
| `naver.txt` | 네이버 스마트에디터에 붙여 넣기 (평문) |
| `meta.json` | 제목 · 요약 · 태그 · 카테고리 |
| `images.txt` | 업로드할 이미지 **순서** |

---

## 티스토리 절차

1. 브라우저 연결 확인 — 연결된 크롬이 없으면 여기서 멈춥니다
2. 글쓰기 화면으로 이동
3. 제목: `meta.json` 의 `제목` (짧은제목 아님 — 검색 수식어가 붙은 쪽)
4. 에디터를 **HTML 모드**로 전환
5. `tistory.html` 전체를 붙여 넣기
6. 노란 점선 상자(`▲ 여기에 … 업로드`)를 위에서부터 찾아,
   그 자리에 `images.txt` 순서대로 이미지를 올리고 상자를 지웁니다
7. 각 이미지의 **대체 텍스트(alt)** 를 상자에 적힌 문구로 채웁니다 — 이미지 검색 유입 경로입니다
8. 카테고리 · 태그 입력 (`meta.json`)
9. **임시저장**
10. 사람이 미리보기로 확인 → 발행

## 네이버 블로그 절차

1. 글쓰기 화면으로 이동
2. 제목 입력
3. `naver.txt` 를 붙여 넣기
4. `[[이미지 삽입: …]]` 표시를 찾아 그 자리에 이미지를 올리고 표시를 지웁니다
5. 표는 붙여 넣기가 깨지므로 **스마트에디터의 표 기능으로 다시 만듭니다**
6. 태그 입력
7. **저장**(임시)
8. 사람이 확인 → 발행

**네이버 주의**
- 같은 문장이 티스토리와 100% 겹치면 양쪽 다 손해입니다.
  도입부와 소제목은 손으로 바꿔 씁니다 (CLAUDE.md 6장 — 문장 재사용 0%).
- 자동 입력 속도가 사람보다 빠르면 탐지에 걸립니다. 천천히, 나눠서 넣습니다.
- 하루에 몰아서 여러 편 넣지 않습니다.

---

## 유튜브 쇼츠 · 틱톡

**API 업로드는 지금 쓰지 않습니다.**

| | 제약 |
|---|---|
| 유튜브 | 미인증 프로젝트가 올린 영상은 **비공개로 잠깁니다**. 풀려면 audit |
| 틱톡 | 미감사 클라이언트의 게시물은 **비공개로 잠깁니다**. Direct Post 는 별도 승인 |

즉 API 를 붙여도 지금은 아무도 볼 수 없는 영상이 올라갑니다.
채널이 자리 잡은 뒤(글 15~25편, 색인 완료) 다시 판단합니다.
그때까지는 `video.mp4` 를 내려받아 앱에서 직접 올립니다.

영상에 음성을 얹는 순서:
1. `narration.txt` 의 (음성) 줄만 음성 서비스에 넣어 mp3 생성
2. 콘텐츠 폴더에 `narration.mp3` 로 저장
3. `python3 scripts/motion.py content/<폴더>` 재실행 — 자동으로 합성됩니다

**음성 고르기.** 무료 기본 음성은 한국 쇼츠에 너무 흔합니다.
그걸 쓰면 "그 목소리 채널" 중 하나가 됩니다. 한 번 고르면 **고정**합니다 —
목소리가 곧 채널 식별자입니다.

---

## 발행 후

- `content/<폴더>/published.json` 에 URL 과 발행 시각을 남깁니다
- 수치가 바뀌면(요율 개정 등) 새 글이 아니라 **이 글을 고칩니다** — CLAUDE.md 5장
