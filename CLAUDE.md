# 마치마자 (Machimaza)

> 이 프로젝트의 **단일 기준 문서**입니다. 별도 가이드를 만들지 말고 이 파일을 갱신하세요.
> 영문 표기는 **machimaza** 로 통일합니다.
>
> **v3 (2026-08-24)** — 14건의 검증 결과를 반영해 전면 재작성했습니다.
> 이전 버전(하루 3~4건 자동 발행, 3개 카테고리, 6개 플랫폼)은 검증에서 실패 패턴으로 판정됐습니다.
> 검증 기록은 §12에 있습니다. **읽지 않고 옛 방식으로 돌아가지 마세요.**

---

## 0. 게이트가 막는 것 (한눈에)

`python3 scripts/verify.py content/<폴더>` 하나가 아래를 전부 검사합니다.
**문서와 코드가 어긋나면 코드가 맞습니다.**

| 검사기 | 막는 것 |
|---|---|
| `check_numbers` | 작년 값을 올해라고 표기 · 원장에 없는 비율 · 산식 불일치 · 폐지된 제도 서술 · 연도 혼동(만원 단위) · 표 열이 서로 같음 |
| `lint_terms` | 등록 용어를 뜻풀이 없이 첫 등장 (`terms.json`) |
| `lint_style` | 표준 표현 미사용 — 빠집니다/씁니다/더합니다 등 (`style.json`) |
| `lint_tone` | 과장·불안 조성 · 의료법 · 금소법 금칙 |
| `lint_sameness` | flow 순서 동일 · layout 3연속 · 여닫는 장치 3연속 · **강조색 3연속** · 문장 재사용 4%↑ |
| `verify` | 표를 위치로 가리킴(왼쪽 열 등) · 제목 공급자 언어 · 본문 6,000자 미만 · 이미지 5장 미만 · 카드 밀도 5% 미만 · flow 에 list 장면 없음 · 관통 단어 없음 · 영상 40~55초 이탈 · 출처 등급·도메인 |

**회귀 테스트가 검사기를 감시합니다** — `test_check_numbers.py` · `test_lint_terms.py`.
CI 는 콘텐츠보다 이 둘을 먼저 실행합니다. 검사기가 죽으면 '통과'는 계속 뜨지만 아무것도 안 걸러집니다.

**원장 4개** — 숫자 `rates.json` · 용어 `terms.json` · 표현 `style.json` · 카테고리 `categories.json`

---

## 1. 무엇을 만드는가

> **건강보험료에 관해, 한국에서 가장 정확한 정보를 쓰는 곳.**

경쟁 상대는 다른 블로그가 아니라 **틀린 숫자**입니다.

### 왜 이게 성립하는가 — 실측

이 니치의 상위 노출 글들이 **지금 이 순간 틀린 숫자를 쓰고 있습니다.**

| 사이트 | 표기된 값 | 2026년 실제 |
|---|---|---|
| qbr.co.kr | "2026년 기준 7.09% / 218.8원 / 12.95%" | 7.19% / 211.5원 / 13.14% |
| policy.ambitstock.com (2026.7 발행) | "208.4원" | 211.5원 (2025년 값) |
| hi.ozrank.net | "자동차가액에 따른 점수" | **2024.2 폐지** |
| **공단 공식 산정방법 페이지** | **"195.8원 / 13,980원 / 10.25%"** | **2024년 값 — 2년 방치** |

`qbr`의 "218.8원"은 **어느 연도에도 존재한 적 없는 숫자**입니다.

**오염원이 최상류에 있습니다.** 공단 static 페이지가 2년 묵은 값을 보여주고, 모두가 그걸 베낍니다.
정확한 값은 **EDI 공지**에 따로 있습니다.

> **우리의 차별점(경쟁자가 못 따라오는 지점): 매년 바뀌는 숫자를 자동으로 추적해 항상 맞게 유지하는 것.**
> 경쟁자가 안 하는 이유는 못 해서가 아니라 **지겨워서**입니다. 우리는 자동화합니다.

---

## 2. 니치 — 내는 돈과 받는 돈

> **살면서 내는 돈과 받는 돈 — 매년 바뀌는 제도 숫자를 원문으로 확인해 정리합니다.**
> (2026-08-24 결정. 이전의 '건강보험료 단일'에서 확장)

### 경계는 주제가 아니라 조건입니다

처음에는 니치를 `건강보험료` 라는 **주제 영역**으로 정의했습니다. 잘못된 정의였습니다.
우리가 이기는 이유는 주제가 아니라 **경쟁자가 작년 숫자를 쓴다**는 것이기 때문입니다.

그래서 경계를 세 조건으로 다시 잡습니다.

| 조건 | 없으면 |
|---|---|
| ① 매년 숫자가 바뀐다 | 갱신 정확도가 무의미해집니다 |
| ② 정부·공공기관 공식 숫자가 있다 | 검산할 원문이 없습니다 |
| ③ 계산이 어렵다 | 사람들이 찾지 않습니다 |

**셋을 만족하면 내는 돈이든 받는 돈이든 절세든 투자든 전부 우리 니치입니다.**
목록은 `categories.json` 이 들고 있고, 게이트가 강제합니다.

### 투자와 절약 — 어디까지 들어오나

| | 포함 | 제외 |
|---|---|---|
| **투자** | ISA 한도·비과세, 연금저축·IRP 세액공제, 청약, 금융소득종합과세, 양도세 | 종목·시점 추천, 수익률 전망, 특정 상품 소개 |
| **절약** | 에너지바우처, 통신비 감면, K-패스, 공과금 감면 | 생활 팁(살림·소비 습관) |

기준은 하나입니다 — **검산할 공식 숫자가 있는가.**
제외 항목은 흥미가 없어서가 아니라 **우리 검증 기계가 돌지 않아서**입니다.
그리고 종목 추천은 **금소법 제22조 주체 요건** 위반 위험이 있습니다.

증거: 2026 세제개편안(2026-08-03) ISA 납입한도 4,000만원 · 비과세 500만원 —
기획재정부 공식 숫자입니다. 투자 이야기지만 우리 모델에 그대로 들어옵니다.

### 척추 하나 + 이슈 대응 둘

검증 기록상 **아무 축도 없이 카테고리를 병렬로 시작한 사례는 성공이 0건**입니다
(단일 집중 첫 클릭 5.2일 vs 병렬 12.1일). 그렇다고 이슈를 놓치면 유입이 안 생깁니다.
둘을 이렇게 갈랐습니다.

| | 역할 | 카테고리 |
|---|---|---|
| **척추** | 꾸준히 아카이브를 쌓음. **검색 유입은 아카이브에서 나옵니다** | 건강보험 |
| **이슈 대응** | 파도가 칠 때 나감 | 세금·절세 / 지원금·환급 |

**배분 척추 5 : 이슈 3 : 갱신 2**

**규칙: 이슈 글은 반드시 척추 아카이브 글로 내부 링크를 겁니다.**
안 걸면 이슈가 지나간 뒤 아무것도 남지 않습니다.

### 자기 대입 축 (6개 · 고정)

축은 주제가 아니라 **오디언스 세그먼트**입니다. 니치를 좁히는 도구지 넓히는 도구가 아닙니다.

**허용 축:** 소득구간 · 연령대 · 가구형태 · 지역 · 직업형태 · 생활패턴
**금지 축:** 자산순위(박탈감) · 질병위험도(진단 단정) · 외모·체형(니치 훼손)

### 확장 규칙

`categories.json` 의 `예정` 카테고리(국민연금·고용보험)는
**척추 50편 + 갱신 루틴 자동화** 이후에 엽니다.
갱신이 멈춘 채 넓히면 **틀린 숫자만 늘어납니다.** 그 순간 우리가 이기려던 상대와 같아집니다.

---

## 3. 콘텐츠 규격

> 이 장의 규칙은 **대부분 게이트가 강제**합니다. 어기면 발행이 막힙니다.
> 예전 3장은 제목 공식 다섯 개가 전부 `총정리`로 끝났는데, 지금 게이트는 그걸 실패로 판정합니다.
> 문서와 코드가 어긋나면 **코드가 맞습니다.**

### 분량과 구성

| 요소 | 기준 | 게이트 |
|---|---|---|
| 본문 | **8,000자 이상** | 6,000자 미만 실패 · 8,000자 미만 경고 |
| H2 | 7개 이상 | — |
| 표 | 2개 이상 | 텍스트 표 0개면 실패 |
| 블로그 이미지 | **5장 이상** | 실패 |
| FAQ | **5문항 이상** | — |
| 인스타 카드 밀도 | 잉크 **6% 이상** | 5% 미만 실패 |
| 영상 길이 | **40~55초** | 벗어나면 실패 |

### 제목 — 독자의 질문 형태로

**금지 (게이트 실패):** `총정리` · `완벽정리` · `판단법` · `알아보기` · `파헤치기` · `모든 것`
공급자의 말입니다. 독자는 그렇게 검색하지 않습니다.

**필요 (없으면 경고):** `얼마` · `언제` · `어떻게` · `되나` · `나요` · `기한` · `신청` · `조건`

```
좋음   퇴직하면 건강보험료 얼마 나오나요｜2026년 임의계속가입 신청 기한 2개월
       건강보험 환급금 얼마 받나요｜2026년 8월 지급분은 2025년 진료비 기준

나쁨   퇴직 후 건강보험료, 임의계속가입 판단법｜2026년 기준 자격·기한·금액 총정리
```

구성: **독자 질문 + 연도 + 관통 단어 + 기한.** 한글 30~45자, 구분자 `｜`.

### 도입부 — 찾으러 온 답부터

인사말·예고("오늘은 ~에 대해 알아보겠습니다")는 상위 페이지에서 **한 건도 발견되지 않았습니다.**
인사이트로 시작하지도 않습니다. **숫자를 첫 문장에 둡니다.**

```
좋음   퇴직 전 월급이 300만원이었다면 임의계속 보험료는 월 107,850원입니다.
나쁨   퇴직하면 건강보험료가 오르는 게 아니라 계산 방식이 바뀝니다.
```

1인칭 경험형은 사용하지 않습니다 — 허구 경험 금지.

### 레이아웃 3종 — `meta.layout`

포스터 구성을 실제로 바꿉니다. 오랫동안 이 필드는 **선언만 되고 아무도 읽지 않아**
모든 글의 포스터가 똑같이 나왔습니다.

| 값 | 포스터 | 언제 |
|---|---|---|
| `list` | 번호 매긴 목록 | 구간별 값이 하나씩 |
| `table` | 표 (2열 대비 가능) | 연도 대비 · 두 제도 비교 |
| `compare` | 값 + 막대 | 크기 차이를 보여줄 때 |

**직전 글과 같으면 경고, 3편 연속이면 실패** (`lint_sameness.py`).

### 강조색 로테이션 — `meta.accent`

색이 카테고리로만 갈리면 **같은 카테고리 글은 전부 같은 색**이 됩니다.
건강보험만 65편 계획이라, 그대로 두면 65편이 한 덩어리로 보입니다.

| 카테고리 | accent 0 | 1 | 2 |
|---|---|---|---|
| 건강보험 | `#2E6B5E` | `#3B6B4A` | `#256A6B` |
| 세금·절세 | `#1F5C8B` | `#35547F` | `#14607E` |
| 지원금·환급 | `#8A5A2B` | `#8A4A32` | `#7C6127` |

**계열 안에서만 돕니다.** 초록 글이 갑자기 빨강이 되지 않습니다 —
§11 의 전제가 "이름을 기억당하기"인데, 매번 다른 얼굴이면 기억할 것이 없습니다.

바꾸는 것과 안 바꾸는 것을 갈라 둡니다.

| | 무엇 |
|---|---|
| **절대 안 바뀜** | 종이색·글꼴·여백·안전영역·워터마크 자리 |
| **회전** | `layout` 3종 · `flow` 장면 순서 · 여닫는 장치 · `accent` |
| **내용이 결정** | 두 해 대비면 `table`, 구간별 값이면 `list`, 크기 차이면 `compare` |

**플랫폼이 처벌하는 건 시각적 템플릿이 아니라 내용의 대량생산입니다**
(유튜브 "unoriginal content" · 틱톡 "고정된 패턴 반복").
그래서 바꿔야 할 것은 색이 아니라 **판단 순서(`flow`)와 숫자**입니다.
색 로테이션은 "열 편이 한 덩어리로 보이는 것"만 막는 보조 장치입니다.

같은 카테고리에서 3편 연속 같은 accent 면 `lint_sameness` 가 경고합니다.
값은 `brand/tokens.css` 에도 있어야 하고 `test_brand.py` 가 대조합니다.

### 반복 방지 — `lint_sameness.py`

첫 글과 두 번째 글이 거의 같은 물건이었습니다. layout·flow 순서·여닫는 장치가 전부 같았습니다.

| 검사 | 판정 |
|---|---|
| flow 장면 순서가 기존 글과 완전히 동일 | 실패 |
| layout 3편 연속 동일 | 실패 |
| 같은 카테고리에서 accent 3편 연속 동일 | 경고 |
| 여는 장치 / 닫는 장치 3편 연속 동일 | 실패 |
| 문장 재사용 4% 초과 | 경고 (3%로 잡았더니 필수 문구만으로 초과했습니다) |

**여는 장치** 3줄 요약 · 질문 표 · 숫자 먼저
**닫는 장치** 정리하면 · 체크리스트 · 한 문장 결론

고지 문구·저자 서명·출처 섹션은 **매번 같아야 하므로** 재사용 계산에서 제외합니다.

### 목차는 고정하지 않습니다

예전 3장에는 12항목 고정 목차가 있었습니다. 통일성 제거 원칙과 정면으로 충돌합니다.
**대신 반드시 들어가야 하는 것만 정합니다.**

| 필수 | 왜 |
|---|---|
| 금액·기준 표 (근거 연도 명시) | 검색 의도의 핵심 |
| **제외 대상 · 흔한 오해** | 상위 글의 차별점. 하위 글에는 없습니다 |
| 신청 방법 | "그래서 뭘 해야 하나"에 대한 답 |
| FAQ 5문항 이상 | 롱테일 흡수 |
| 공식 출처 + 최종 확인일 | E-E-A-T |

---

## 4. 수치 원장 — 이 프로젝트의 심장

**`rates.json` 하나가 모든 숫자의 유일한 출처입니다.**

```json
{
  "2026": {
    "요율": 0.0719,
    "재산보험료부과점수당": 211.5,
    "장기요양보험료율": 0.009448,
    "장기요양_건보료대비": 0.131405,
    "보수월액보험료_상한": 9183480,
    "지역세대_상한": 4591740,
    "하한": 20160,
    "재산_기본공제": 100000000,
    "소득평가율": { "이자": 1.0, "배당": 1.0, "사업": 1.0, "기타": 1.0,
                    "근로": 0.5, "연금": 0.5 },
    "출처": "https://edi.nhis.or.kr/portal/images/popup/20251204_pop01longdesc.html",
    "출처명": "국민건강보험공단 EDI 2026년도 보험료율 인상 안내 (2025-12-04)",
    "확인일": "2026-08-24"
  }
}
```

### 절대 규칙

1. **글·카드·영상의 모든 숫자는 `rates.json`에서 옵니다.** 본문에 숫자를 직접 타이핑하지 않습니다.
2. **연도를 명시하지 않은 숫자는 쓰지 않습니다.**
3. 모든 수치 옆에 **근거 연도 + 최종 확인일**을 표기합니다.
4. `check_numbers.py`가 본문의 모든 숫자를 `rates.json`과 대조합니다. 불일치면 발행 불가.

### `check_numbers.py` — 차별점을 강제하는 장치 (2026-08-24 구현)

이 프로젝트의 우위는 "숫자가 맞다" 하나뿐입니다. 사람의 주의력에 맡기면 반드시 새어 나가므로
게이트가 기계적으로 막습니다. 검사 4종:

| # | 검사 | 잡는 것 |
|---|---|---|
| 1 | **과거연도 값 오염** | 작년 값을 올해 값으로 씀. 면제 판정은 줄 전체가 아니라 **숫자 바로 앞 12자**로 좁혀, 뒤쪽 연도 표기가 앞쪽 오염을 가리지 못하게 함 |
| 2 | **올해 값 적극 확인** | "2026년 …요율은 X%" 라고 썼다면 X는 반드시 원장의 올해 값 |
| 3 | **유령 비율·금액** | 원장·`data.json` 어디에도 없는 %와 원 금액. 예외는 `data.json`의 `allow_pct`·`derived`에 **산식과 함께** 등록해야 통과 |
| 4 | **폐지 제도 서술** | 자동차 부과처럼 폐지된 것을 현행처럼 설명 |

**검증:** 경쟁자들이 실제로 발행한 오류(7.09% / 218.8원 / 12.95% / 자동차 부과)를 본문에 재현해
넣었더니 **4종 전부 검출**됐습니다. 정상 원고는 통과. 이 테스트를 회귀 테스트로 유지합니다.

`derived` 규칙 — 파생 금액(예: 장기요양보험료)은 "약 14,000원" 같은 어림수로 쓰지 않습니다.
`data.json`의 `derived`에 산식을 적고 게이트가 계산해 검산한 정확한 값(14,170원)을 씁니다.
어림수는 경쟁자와 구별되지 않고, 갱신 시 무엇이 바뀌었는지 추적할 수 없습니다.

### 자주 틀리는 3가지 (경쟁자들이 실제로 틀린 것)

| 항목 | 틀린 값 | 맞는 값 |
|---|---|---|
| 근로·연금 소득 평가율 | 30% | **50%** (2022.9 개편) |
| 자동차 부과 | 점수 산정에 포함 | **2024.2 완전 폐지** |
| 부과점수당 금액 | 195.8 / 208.4 / 218.8 | **211.5원** (2026) |

---

## 5. 갱신 — 차별점의 실체

### 감시 대상

| 소스 | 대상 | 방법 |
|---|---|---|
| `edi.nhis.or.kr` 공지 | 요율·점수당 금액·상하한 | 분기 1회 수동 확인 (가장 정확, 가장 안 알려짐) |
| **`legalize-kr` 저장소** | 국민건강보험법 시행령 개정 | **커밋 감시** ← 권장 |
| 복지부 보도자료 | 부과체계 개편 | 월 1회 |

**법제처 OPEN API를 직접 치지 않습니다.** 검증에서 세 가지 함정이 확인됐습니다 —
Referer 헤더 필수 / 브라우저 UA 필수 / **클라우드 IP에 안티봇 JS 반환**.
실제로 매일 이 API를 돌리는 프로덕션 파이프라인은 전부 self-hosted 러너를 씁니다.

대신 **`legalize-kr`이 매일 전체 법령을 수집해 Git 커밋으로 만들어둡니다.**
`kr/국민건강보험법/시행령.md` 커밋을 감시하면 API를 안 써도 됩니다.

### 갱신이 신규보다 쌉니다

구조·출처·이미지가 이미 있고 숫자만 바꾸면 됩니다. **30분이면 끝납니다.**
그리고 구글은 갱신된 기존 문서를 신규 문서보다 **더 빨리, 더 높게** 올려줍니다.

> **6개월 차부터 갱신이 신규만큼 중요해집니다.**
> `stale-audit.yml`이 매주 월요일 갱신 대상을 이슈로 띄웁니다.

### 최종 수정일을 노출합니다

모든 글 상단에 표기합니다. 경쟁자 중 아무도 못 합니다.

> **2026년 기준 · 최종 확인 2026-08-24 · 출처: 공단 EDI 공지(2025-12-04)**

---

## 6. 발행 리듬 — 주 2~3건

| | v2 (폐기) | v3 |
|---|---|---|
| 발행 | 하루 3건 / 주 21건 | **주 2~3건** |
| 검수 | 기계 게이트만 | **기계 게이트 + 사람 최종 승인** |
| 카테고리 | 3개 | **1개** |

무편집 AI 대량 발행의 실측 결과가 이 결정의 근거입니다.

> 2,000편 16개월 → 누적 노출 109만 / 클릭 **1,381건** (CTR 0.13%)
> 반면 **편집한 AI 6편 → 6편 중 3편 톱10**

> **"한 편의 진짜 기사가 다섯 편의 얇은 기사보다 훨씬 낫다."**
> — 2,300편을 자동 발행하고 트래픽 90%를 잃은 운영자의 최종 결론

**AI 비중 50% 미만을 유지합니다.** 상위 1~3위의 82.2%가 이 구간입니다.
AI가 못 만드는 것을 반드시 넣습니다 — **원문에서 직접 확인한 날짜·금액**, 제외 사유, 흔한 오해.

### 트랙별 발행

| 트랙 | 주간 | 역할 |
|---|---|---|
| 티스토리 (자체 도메인 검토) | 2~3건 | 구글 검색 · 애드센스 |
| 네이버 블로그 | 2건 | 네이버 검색 · 애드포스트 |
| 인스타 · 쓰레드 | 2~3건 | **브랜드** (트래픽 아님) |
| 쇼츠 · 틱톡 | **보류** | §9 참조 |

**네이버 유입은 네이버 블로그로만.** 외부 웹사이트 노출 비중이 9.5%라 자체 도메인으로는 진입로가 없습니다.

### 중복 콘텐츠 회피 — 문장 재사용 0%

| | 티스토리 | 네이버 |
|---|---|---|
| 앵글 | 데이터·비교·체계적 정리 | 절차·경험 서술 |
| 길이 | 8,000자+ | 2,000자 내외 |
| 제목 | 완전히 다른 문구 (키워드만 공유) |
| 이미지 | 다른 파일 |
| 발행 순서 | **먼저** (구글이 최초 색인본을 원본으로 인정) | 색인 확인 후 |

**부분 리라이팅으로는 회피되지 않습니다.** "다른 질문에 답하는 두 글"로 설계합니다.
그리고 **두 글을 서로 링크하지 않습니다** — 같은 세트로 묶는 신호가 됩니다.

---

## 7. 이미지

이미지는 SNS용 부산물이 아니라 **블로그 품질의 핵심 요소**입니다.
티스토리 bad 등급 조건에 "이미지 없는 텍스트만의 콘텐츠"가 있습니다.

| 위치 | 이미지 |
|---|---|
| 상단 대표 | 포스터 1장 |
| H2 섹션마다 | 카드 1장 (최소 3장) |
| 본문 중간 | 데이터 차트 1장 (수치 3개 이상일 때) |
| 하단 | 요약 카드 + `@machimaza` 유도 |

**한 건당 본문 최소 5장.** 규격 1080 × 1350 (4:5), 인스타와 공용.
`images/alt.txt`를 자동 생성해 업로드 시 첨부합니다 — 이미지 속 글자는 검색엔진이 못 읽으므로
**alt가 유일한 텍스트 신호**입니다.

**공공누리는 제1유형만 사용합니다.** 애드센스 수익이 있으면 "상업적 이용"이라
제2·4유형은 사용 불가입니다. 보도자료 본문이 공공누리여도 그 안의 사진·인포그래픽은
적용 제외인 경우가 많으니, 애매하면 **수치만 인용해 직접 디자인**합니다.

---

## 8. 톤 & 법적 규제

### 톤

**지킬 것** — 존댓말, 한 문장 40자 이내, 결론 먼저.
독자를 '여러분'이 아니라 **상황**으로 부릅니다 → "퇴직을 앞두고 있다면"

**피할 것** (`lint_tone.py`가 자동 차단)
- 과장 후킹: "충격", "이것만 알면", "99%가 모르는"
- AI 상투구: "~에 대해 알아보겠습니다", "결론적으로", "안녕하세요"
- 근거 없는 불안 조성, 이모지 남발

### 법적 규제 — 형사처벌 영역

SEO 페널티는 회복되지만 형사처벌은 회복되지 않습니다.

| 법령 | 형량 | 우리에게 |
|---|---|---|
| **식품표시광고법 제8조①** | **10년 / 1억원** | 수범자가 **"누구든지"**. 협찬 없어도 대상 |
| 의료법 제27조③ 유인·알선 | 3년 / 3천만원 | 병원 링크·할인코드 금지 |
| 금소법 제22조 | 1억원 과태료 | **개인은 금융상품 광고 주체 불가** |

**절대 금지**
- `[질병명] + [예방·치료·개선·효능]` 조합 — 문장 단위로 금지
- 항암 · 면역력 강화 · 염증 제거 · 디톡스 · 부작용 없음 · 100% 안전
- 특정 **병원·의사·시술명** 지목, 예약 링크·할인코드
- "이 증상이면 ○○병입니다" 진단성 서술
- **특정 은행·보험 상품 소개** (금소법 — 대가성 표시를 해도 주체 요건 위반)
- 개별 상담·DM 자문

**좋은 소식 두 가지**
- 애드센스·애드포스트 수익만 있으면 **유사투자자문업 신고 불필요** (금감원 공식 기준)
- 애드센스 배너만 있으면 **대가성 표시 의무 없음** (콘텐츠와 무관한 자동 게재)

### 고정 문구

건강 카테고리:
> 이 글은 일반적인 정보 제공을 위한 것이며, 개인의 상태에 따라 다를 수 있습니다.
> 정확한 보험료와 자격은 국민건강보험공단(1577-1000)에 확인하시기 바랍니다.

저자 서명 (E-E-A-T 신호 — 구글이 보는 신호 중 유일하게 비어 있던 축):
> — 마치마자 · 정부·공공기관 원문을 직접 확인해 정리하며, 제도 개정 시 갱신합니다.

### 표현 — `style.json` + `lint_style.py`

> **일괄 치환은 금지입니다.** 2026-08-24 표현 교체를 스크립트로 한 번에 돌렸다가 두 건이 깨졌습니다.
> `절반으로 나눕니다` → `절반으로 구분합니다` (뜻 파손)
> `1년을 넘으면` → `1년을 초과하면` (**법 요건은 '1년 이상'** — 정확히 1년인 경우가 갈립니다. 사실 오류)
> 그래서 교체 목록에는 문맥에 안전한 것만 남기고, 판단이 필요한 것은 `_확인만` 으로 옮겼습니다.
> 린터는 **표시만 하고 고치지 않습니다.**


**`terms.json` 과 방향이 반대입니다.**

| | 무엇을 | 예 |
|---|---|---|
| `terms.json` | 오독 위험이 있는 제도어를 **풀어씁니다** | 섬·벽지 → 섬·벽지(외딴 지역) |
| `style.json` | 널리 사용하는 표준 표현을 **그대로 사용합니다** | 빠집니다 → **제외됩니다** |

모순이 아닙니다. 기준은 하나입니다 —
**독자가 고지서와 공단 안내에서 실제로 만나는 말인가.**

`제외`·`사용`·`합산`·`초과`는 만나는 말이고, `섬·벽지`는 만나지만 뜻이 안 통하는 말입니다.

**교체 대상 (게이트 실패)**

| 사용하지 않음 | 사용 |
|---|---|
| 빠집니다 | **제외됩니다** |
| 씁니다 · 쓰는 | **사용합니다 · 사용하는** |
| 더합니다 · 더해서 | **합산합니다 · 합산해서** |
| 넘어야 · 넘으면 | **초과해야 · 초과하면** |
| 봅니다 · 셉니다 | **판단합니다 · 산정합니다** |

**유지하는 것** — `돌려받습니다`(환급받습니다보다 뜻이 빠름) · `깎아줍니다`(경감을 괄호 병기).
제목과 소제목에는 제도어(`환급`·`제외 대상`·`신청 방법`)를 우선해 검색어를 확보합니다.

### 용어 — `terms.json` + `lint_terms.py` (2026-08-24 신설)

**계기.** `섬·벽지 거주 | 50%` 를 표에 그대로 넣고 게이트를 통과시켰습니다.
법조문조차 `섬·벽지(僻地)` 라고 한자를 병기하는 단어인데, 그 괄호를 떼서
**원문보다 불친절한 글**을 만들었습니다. 사용자가 "벽지가 뭐냐"고 물어서 발견했습니다.

**진단.** 게이트가 검사하던 것은 전부 "틀렸는가" 였습니다. `check_numbers`(숫자),
`lint_tone`(과장·의료법), `verify`(출처·축·고지문구). **"읽히는가"를 보는 장치가 하나도 없었습니다.**
정확도만 차별점으로 정의한 탓입니다. 그 정의가 틀렸습니다 —
**틀린 글과 안 읽히는 글은 독자에게 똑같이 쓸모없습니다.**

**규칙**

| # | 내용 |
|---|---|
| 1 | `terms.json` 이 용어 표기의 유일한 출처. 표기 / 쉬운말 / 카드용 / 나레이션용 / 오독위험 / 근거 조문 |
| 2 | 등록 용어는 본문 **첫 등장 앞뒤 80자 안**에 뜻풀이가 있어야 함. 없으면 게이트 FAIL |
| 3 | 표기는 **법 표기를 앞에, 쉬운 말을 괄호에.** 고지서에서 만날 단어를 못 알아보면 안 됨 |
| 4 | 뜻풀이는 표 안에 넣음 — 독자는 본문보다 표를 먼저 읽음 |
| 5 | 카드 라벨(8자 제약)은 법 표기를 버리고 뜻으로 대체 (`보수월액 250만원` → `월급 250만원`) |
| 6 | AI 나레이션은 한자어를 순화어로 읽음 — 귀로는 동음이의어가 구분되지 않음 |

**한자 병기 = 공짜 위험 신호.** 법령 원문에서 `단어(漢字)` 패턴을 긁으면
**입법자가 "이건 안 읽힌다"고 판단한 단어 목록**이 그대로 나옵니다. 사전 확장의 1순위 재료입니다.

**등록 시점의 오독 위험 상위 3개**

| 용어 | 오독 | 왜 치명적인가 |
|---|---|---|
| 세대 | 世代(generation) | 지역보험료 **합산 단위**. 오독하면 금액 이해가 통째로 어긋남 |
| 소급 상실 | 단순 "해지" | 실제로는 **처음부터 없던 것으로 되돌아감**. 위험도가 전달되지 않음 |
| 섬·벽지 | 壁紙(도배지) | 50% 경감 대상인데 본인이 해당되는지 판단 자체가 불가능해짐 |

---

## 9. 화면 규칙 — 영상·카드·블로그 공통

> 이 장은 세 매체에 **전부** 적용됩니다.
> 첫 글에서 나온 지적이 매체마다 따로 터진 게 아니라, 같은 원인이 세 곳에서 드러난 것이었습니다.

### 하나의 흐름, 세 개의 렌더

`data.json` 의 `flow.scenes` 하나를 영상과 인스타 카드가 함께 그립니다.
블로그는 같은 순서를 따르되 전부를 담습니다.

**표준 흐름: 자격 → 금액 → 기한 → 신청 방법 → 지금 할 일**

첫 영상과 첫 카드는 둘 다 소득구간 5개를 나열하고 끝났습니다.
사용자 지적: *"가격 비교만 하고 끝임. 어떻게 신청하는지 그런 정보들은 없네?"*
원인은 둘 다 `items` 를 기계적으로 찍은 것이었습니다.

**그래서 게이트가 막습니다** (`verify.py`):

| 검사 | 실패 조건 |
|---|---|
| flow 존재 | `flow.scenes` 없음 |
| 최소 길이 | 5장면 미만 |
| 시작 | 첫 장면이 `hook` 이 아님 |
| **행동 장면** | `list` 장면이 하나도 없음 = "그래서 뭘 해야 하나"에 답이 없음 |
| **가격표 방지** | 장면이 `compare`/`table` 로만 이루어짐 |
| 자막·음성 | `screen` 또는 `voice` 가 빔 |

### 상단 배지 — 좌 분야 / 우 관통 단어

    ( 건강보험 )                    [ 임의계속가입 ]
      테두리 · 조용                    진한 배경 · 강조

**둘을 같이 두면 한 줄로 읽힙니다** — "건강보험 얘긴데, 그중 임의계속가입 얘기".
분야만 있으면 무슨 얘긴지 모르고, 관통 단어만 있으면 어느 제도인지 모릅니다.

- **모양은 반드시 다르게.** 같은 pill 두 개는 반복 요소로 읽혀 둘 다 안 보입니다
- **관통 단어 고르는 기준: 공단에 전화해서 말해야 하는 단어.**
  이것만 기억하면 나머지는 물어보면 되기 때문입니다. `meta.throughline` (게이트 필수)
- **영상·포스터·카드가 같은 규칙**을 씁니다. 다르면 같은 채널로 안 보입니다

### 표에서 강조하는 열은 작성자가 정합니다 — `accent_col`

기본값(마지막 열)에 맡겼더니 **설명은 2025년을 가리키는데 강조색은 2026년에** 붙었습니다.
글과 눈이 따로 놀면 표를 오래 보게 되고, 그만큼 틀리게 읽습니다.

**"지금 봐야 할 값"이 있는 열에 강조를 둡니다.** 연도순 배열은 유지하되 강조만 옮깁니다.

### 표는 위치가 아니라 이름으로 가리킵니다

```
나쁨   지금 확인해야 할 것은 왼쪽 열입니다.
좋음   지금 확인해야 할 것은 「2025년 진료분」입니다.
```

같은 표가 **블로그·카드·영상에서 각각 다르게 배치**되고, 모바일에서는 열이 접힙니다.
'왼쪽'이 어디인지 매체마다 달라집니다. 그리고 독자가 열을 세어야 합니다.

**헤더에 적힌 이름을 그대로 인용합니다.** 게이트가 위치 표현을 실패로 막습니다
(`첫 열` · `왼쪽 열` · `마지막 열` · `두 번째 열` 등).

### 라벨 — 그 화면만 보고 뜻이 통해야 합니다

**본문을 읽어야 이해되는 라벨은 실패한 라벨입니다.** 영상·카드는 문맥이 없고,
블로그도 표는 건너뛰며 읽힙니다.

| 고친 것 | 왜 |
|---|---|
| `깎기 전` → `월급 × 7.19%` | **무엇을** 깎기 전인지 화면 어디에도 없었음 |
| `산정보험료` → `월급 × 7.19%` | 15줄 위 산식 블록을 읽어야 통했음 |
| `1등급 30% / 2등급 20%` → `소득·재산이 적을수록 30% → 20% → 10%` | 무슨 등급인지 표에 없었음 |
| 배지 `건강` → `건강보험` | 건강 콘텐츠로 오해됨. 3카테고리 시절 잔재 |
| `고지 후 2개월 이내` → `첫 고지서 납부기한 +2개월` | 무슨 고지인지 없었음 |
| `싸다 / 쌉니다` → `저렴하다` | 돈 이야기에서 가볍게 읽힘 |

### 같은 말을 두 번 하지 않습니다

- 배지가 말한 것을 화면이 또 말하지 않습니다 (제목 장면 kicker 제거)
- 전용 장면에서 크게 말하는 것을 같은 화면의 배지가 또 말하지 않습니다 (기한 배지 제거)
- **음성은 화면 자막을 읽지 않습니다.** 화면=사실, 음성=해석.
  `build_narration.py` 가 Dice 계수 0.45 로 판정

### 상시 노출할 자격이 있는 것은 하나뿐

기한처럼 **한 번 꽂아야 하는 정보**를 모든 장면에 띄우면
세 번째 장면부터 아무도 읽지 않습니다. 전용 장면에서 크게 한 번 말하고, 배지는 비웁니다.
상시 자리는 관통 단어에게 줍니다.

### 이미 아는 숫자와 모르는 숫자를 갈라 보여줍니다

사용자 지적: *"실제 월급으로 받아서 냈던 금액과의 비교도 추가되면 좋지 않을까?"*

검산해 보니 **임의계속 금액 = 재직 중 본인부담분** 이었습니다. 5구간 전부 일치합니다
(재직 `보수월액 × 3.595%` / 임의계속 `보수월액 × 7.19% × 50%`).
50% 경감이 그렇게 설계된 것입니다.

두 열을 나란히 놓는 것이 가장 강한 메시지가 됩니다 — **"금액이 바뀌지 않는다."**
그리고 진짜 구조가 드러납니다.

| | 알 수 있나 |
|---|---|
| 임의계속 금액 | **이미 알고 있음** — 재직 중 내던 그 금액 |
| 지역보험료 | **고지서를 봐야 앎** — 재산·가구원 합산이라 미리 계산 불가 |

판단이 어려운 이유는 계산이 어려워서가 아니라 **한쪽을 모르기 때문**입니다.
계산기를 만들지 않기로 한 근거이기도 합니다 — 계산 안 되는 쪽이 문제라 계산기로는 안 풀립니다.

`check_numbers.py` 가 `items[].wage` 로 두 금액을 각각 검산하고,
**둘이 다른데 화면이 "금액이 바뀌지 않는다"고 말하면 실패**시킵니다.

### 매체마다 밀도가 다릅니다 (2026-08-24 실측)

**인스타 카드에는 음성이 없습니다.** 영상이 음성으로 말하는 해석을 카드는 글로 실어야 합니다.
처음에는 `voice` 를 카드에서 통째로 버리고 있었고, 그래서 카드가 비었습니다.

측정값 (잉크 = 배경이 아닌 픽셀 비율)

| | 처음 | 지금 |
|---|---|---|
| 인스타 카드 | **4.2%** (화면의 95%가 빈 공간) | 7% |
| 쇼츠 안전영역 | 7.1% (최소 3.6%) | 9.7% (최소 8.0%) |

**고친 방법 — 장식이 아니라 정보로 채웁니다**

| 넣은 것 | 왜 |
|---|---|
| `voice` 를 카드에 글로 | 인스타는 소리가 없습니다 |
| 장면별 `points` (2~3줄) | 영상은 시간이, 카드는 지면이 남습니다. 같은 flow, 다른 밀도 |
| 진행 표시 (3/7) · 진행 막대 | 몇 장 남았는지가 완주율을 좌우합니다 |
| 바닥 근거 줄 (연도 기준 · 기관) | 빈 곳에 신뢰 정보를 넣습니다 |
| 표지 확대 | 스크롤이 멈추는 자리인데 본문 카드와 크기가 같았습니다 |

**게이트가 지킵니다** — 카드 잉크 5% 미만이면 실패, 평균 6% 미만이면 경고.

발견한 버그: 요점을 넣자 본문이 바닥의 근거줄·자막 영역까지 내려와 글자가 포개졌습니다.
안전영역 안에서 본문이 끝나는 지점(y≈1200)을 `padding-bottom` 으로 못 박았습니다.

### 영상 여백 — 유튜브 안전영역을 따릅니다

`motion.py` 의 `.wrap` 여백은 눈대중이 아니라 유튜브가 밝힌 값입니다.

| 방향 | 유튜브 권장 | 우리 값 | 무엇이 덮나 |
|---|---|---|---|
| 위 | 288px | 210px | 상단 UI |
| **오른쪽** | **192px** | **190px** | **좋아요·댓글·공유 버튼** |
| 아래 | 672px | 720px | 제목·설명·채널명 |
| 왼쪽 | 48px | **190px** | — |

**왼쪽을 오른쪽에 맞춥니다.** 예전에 왼쪽 100 / 오른쪽 190 이었는데,
글이 왼쪽으로 밀려 보였습니다. 좁은 쪽을 넓혀 대칭으로 만들었습니다 —
오른쪽을 줄이면 버튼에 가리므로 반대로 하면 안 됩니다.

본문·자막·출처는 **가운데 정렬**입니다. 다만 **목록 항목은 왼쪽 정렬을 지킵니다** —
여러 줄을 가운데로 맞추면 줄마다 시작점이 달라져 눈이 매번 새로 찾아야 합니다.

### 세로 자리 — 넘치면 렌더가 멈춥니다

| 자리 | y | 무엇 |
|---|---|---|
| **워터마크** | **48~88** | **@machimaza** — 왼쪽 위 모서리 (x=48) |
| 상단 줄 | 210~284 | 분야칩(왼쪽) · 관통 단어(오른쪽) |
| 진행바 | 322 | |
| **본문** | **334~1200** | 여기를 넘으면 안 됩니다 |
| 출처줄 | 1208~1248 | 근거 연도 · 기관 |
| 자막 | 1336~1460 | |

**채널명은 왼쪽 위 모서리, 워터마크 자리입니다.** 네 번 옮긴 끝입니다 —

| 어디에 뒀었나 | 왜 옮겼나 |
|---|---|
| 바닥 (bottom:625) | 본문이 길어지면 그 위로 글이 내려앉아 **겹쳤습니다** |
| 상단 줄 가운데 (칩 사이) | 분야칩과 관통 단어 **사이에 묻혔습니다** |
| 맨 위 가운데 (y=100) | 제목과 한 덩어리로 읽혔습니다 |
| **왼쪽 위 모서리 (48,48)** | **지금** |

x=48 은 유튜브가 밝힌 **왼쪽 안전영역 그대로**입니다 — 왼쪽은 원래 48px 만 비우면 됩니다.
y=48 은 상단 권장(288)을 크게 벗어납니다. **그래도 두는 이유**: 요즘 폰은 9:19.5 라
9:16 영상이 위아래로 레터박스되고, 유튜브 상단 아이콘(뒤로가기·검색·더보기)은
그 **검은 띠에** 얹힙니다. 9:16 에 가까운 화면에서는 뒤로가기 버튼에 가릴 수 있는데,
**가려도 잃는 것이 워터마크 하나뿐**이라 감수합니다.
`FIT_JS` 가 모서리 밖(x·y < 36)이나 분야칩 줄 침범을 잡습니다.

**영상·포스터·카드가 같은 자리를 씁니다** — 셋 다 왼쪽 위 (48, 48).
`motion.py` 와 `pipeline.py` 가 각각 그리지만 자리는 하나입니다.
한쪽만 옮기면 같은 글이 매체마다 다른 얼굴이 됩니다.

**핸들은 매체마다 다릅니다.** 인스타·쓰레드만 `@machi_maza` 입니다 —
`machimaza` 가 이미 쓰이고 있어서 밑줄을 넣었습니다.

| 무엇 | 어디로 | 워터마크 |
|---|---|---|
| 포스터 | 블로그 | `@machimaza` |
| 카드 | 인스타 캐러셀 | `@machi_maza` |
| 영상 | 유튜브·틱톡 | `@machimaza` |
| 영상 | 쓰레드·릴스 | `@machi_maza` |

**같은 영상이 양쪽으로 갑니다.** 그래서 `motion.py` 는 `--channel` 을 받고,
`publish.yml` 이 `target` 을 보고 넘깁니다. 값은 **`channels.json` 이 유일한 출처**입니다 —
예전에는 `publish_youtube.py`·`docs/index.html`·`BRAND.md` 세 곳에 흩어져 있었습니다.
`test_brand.py` 가 코드에 핸들을 직접 적으면 CI 를 멈춥니다.

`motion.py` 의 `FIT_JS` 가 **장면마다 실제 상자를 재고**, 본문이 출처줄·진행바를
침범하거나 채널명이 칩에 붙으면 그 자리에서 멈춥니다. 브라우저는 넘친 글도 그대로
그려주기 때문에 **"그려졌다"는 아무 증거가 못 됩니다.**

### 작은창의 검은 여백은 고치지 않습니다

9:16(1080×1920)은 유튜브 권장 규격인데, 요즘 폰 화면은 약 9:19.5 로 더 깁니다.
그래서 작은창처럼 화면 비율에 맞추는 곳에서는 위아래가 남고 유튜브가 검게 채웁니다.
**자르지 않고 남기는 것이라 우리 영상 문제가 아닙니다.**

폰 카메라로 찍은 영상은 화면 그대로(1080×2340)라 꽉 찹니다. 우리도 늘릴 수 있지만
권장 규격을 벗어나고, 안전영역을 다시 계산해야 하고, 인스타·틱톡(9:16 표준)과
따로 만들어야 합니다. **조회는 대부분 전체화면 피드에서 일어납니다.** 남는 장사가 아닙니다.

### 매체별로 다른 것

| | 블로그 | 인스타 카드 | 영상 |
|---|---|---|---|
| 담는 양 | 전부 | flow 요약 | flow 요약 |
| 규격 | — | 1080×1350 (4:5) | 1080×1920 · 40~55초 |
| 라벨 길이 | 길어도 됨 | 8자 내외 | 8자 내외 |
| 음성 | — | — | 있음 (화면과 다른 말) |
| 발행 | 👤 티스토리·네이버 (손으로) | 👤 앱 업로드 | 🤖 워크플로 (유튜브·쓰레드) |

---

## 10. 영상

### 왜 이렇게까지 하는가

**AI 음성 + 정지 이미지 + 변환 없음**은 정책이 정면으로 지목한 조합입니다.

> 틱톡 Creator Rewards unoriginal 플래그 대상:
> "**AI 음성이 텍스트를 읽고 변환이 거의 없으면** 오리지널 인풋이 최소한인 것으로 읽혀 플래그"

유튜브는 **채널 전체 패턴**을 보고 광고 제한 → 수익화 중지 → 채널 폐지로 단계 집행합니다.
2026년 1월 상위 100개 AI 채널 중 **16개가 조치**(11개 완전 삭제),
**한국발 AI 슬롭 채널이 누적 조회 84.5억으로 세계 1위**라 감시가 집중됩니다.

### 5가지 조건과 현재 상태

| # | 조건 | 이유 | 상태 |
|---|---|---|---|
| 1 | **실제 모션** — 숫자 카운트업·막대 성장·요소 등장 | 정지 슬라이드쇼가 플래그 대상. 모션이 "변환"의 증거 | **완료** |
| 2 | **구조 로테이션** (`flow`/`countdown`/`compare`/`step`) | "영상 간 고정 패턴 반복" 회피 | **완료** |
| 3 | **40~55초** | 1분 이상이 더 자주 플래그됨. **리워드를 포기합니다** | **완료** (게이트 판정) |
| 4 | **자막 ≠ 화면 텍스트** | 화면=사실, 자막=해석 | **완료** |
| 5 | **음성 캐릭터 고정 + 흔한 무료 음성 회피** | 피로도 지적이 언론에 나옴 | **사람이 선택** |

### 구현 — `motion.py`

zoompan(느린 확대)은 **버렸습니다.**
**채택: t → 프레임 순수 함수.** CSS 애니메이션을 '재생'하지 않고
프레임마다 `window.draw(t)` 로 그 시점 상태를 직접 그린 뒤 스크린샷합니다.
타이밍이 결정적이라 CI에서 같은 결과가 나옵니다.
움직이는 구간만 찍고 멈춘 구간은 마지막 프레임을 늘립니다.

실측: 1080×1920 **프레임당 74ms** (45초 ≈ 100초 렌더).

### 나레이션은 생성하지 않고 검사합니다

본문을 기계 치환해 대본을 만들려다 `경감받아` → `깎아주는받아` 가 나왔습니다.
한국어 활용이 깨집니다. 대본은 `flow.scenes[].voice` 에 사람이 쓰고,
`build_narration.py` 는 길이(초당 5.5음절)·화면 중복·오독 위험 용어만 판정합니다.

### 원본 기여를 표면에 드러냅니다

유튜브 정책이 요구하는 건 **"오직 제작자만이 만들 수 있는 원본 기여"**입니다.
우리에게 그건 **tier 1 출처 확인**과 **연도 기준 명시**입니다.

### 도입부 — 벨 → 집중시키는 말 → 본문

`data.json` 의 `flow.opener` 에 한 줄을 넣으면 맨 앞에 붙습니다.
질문으로 열어 "내 얘기인가?" 하고 멈추게 하는 자리입니다.

첫 장면을 `LEAD_IN`(4.2초)만큼 길게 잡아 **벨과 첫 대사가 겹치지 않게** 합니다.
이걸 안 두면 둘 다 안 들립니다. 0.9초에 도입 대사, 4.2초에 본문 첫 마디.

### 소리 — 목소리 하나, 배경음 하나

목소리 설정은 `BRAND.md` 6장에 고정돼 있습니다 (현수 · `+15%` · `+10Hz` · azure).
**채널의 얼굴이라 편마다 바꾸지 않습니다.**

| 구간 | 크기 | 뜻 |
|---|---|---|
| 대사 | −19 dB | 기준 |
| 빈 구간 배경음 | −24 dB | 들리되 앞에 안 나섬 |
| 알림음 | −19 dB | 또렷하게 |

- 배경음은 `sidechaincompress` 로 **대사가 나올 때만 물러납니다.**
  그냥 겹치면 대사가 묻히고, 처음부터 작게 깔면 빈 구간이 다시 조용해집니다
- **알림음은 더킹을 거치지 않습니다.** 시작음이 첫 대사와 겹치는데 같이 눌리면 안 들립니다
- 곡을 바꿔도 `loudnorm` 이 같은 크기로 맞춥니다. 귀로 다시 맞출 일이 없습니다
- 음원 고르는 법과 라이선스는 `BGM.md`

### 순서와 중단 조건

- **쇼츠 먼저, 틱톡은 나중.** 틱톡 Creator Rewards 가 더 엄격합니다
- **틱톡 unoriginal 플래그 1회 또는 유튜브 광고 제한 통보 시 영상 트랙 즉시 중단**

---

## 11. SNS 채널과 발행 경로 (자동 업로드 포함)

**트래픽 채널이 아닙니다.** AI 시대의 트래픽은 링크 클릭이 아니라
**"이름을 기억당한 뒤 직접 검색"**으로 옵니다.

| 근거 | 수치 |
|---|---|
| 브랜드 멘션의 AI 가시성 상관 | **0.664** (백링크 0.218) |
| 브랜드 쿼리는 AI Overview 가 있어도 CTR | **+18.68%** |
| AI 인용 브랜드의 오가닉 CTR | **+35%** |

### 자동 업로드 — 어디가 되고 어디가 안 되나

| 플랫폼 | 올리기 | 공개하기 | 사정 |
|---|---|---|---|
| **쓰레드** | ✅ 자동 | ✅ 자동 | 본인 계정은 Meta 앱 검수 불필요 |
| **유튜브** | ✅ 자동 | 👤 손으로 | 앱 검수 전엔 **비공개로만** 올라감 |
| **틱톡** | ✅ 자동 | 👤 손으로 | 심사 전엔 본인만 보기 |
| **인스타** | 🟡 | 👤 | 페이스북 페이지 연결 + 검수 필요 |
| 티스토리 | ❌ | ❌ | Open API 2024.2 종료 |
| 네이버 블로그 | ❌ | ❌ | 글쓰기 API 없음 |

**티스토리·네이버를 브라우저 자동화로 밀지 않습니다.** 두 곳 다 약관에서
자동화를 금지하고, 걸리면 계정 정지입니다. 애드센스를 붙일 블로그를
그렇게 잃을 수 없습니다. 이 둘은 `publish/` 키트로 손으로 올립니다.

**"올리는 건 자동, 공개는 사람"이 오히려 맞는 순서입니다.** 어차피 발행 전에
사람이 봐야 하는 항목(이미지 저작권·과장 뉘앙스·YMYL)이 남아 있습니다.

- 발행은 **미리보기가 기본**입니다. `publish: true` 여야 실제로 올라갑니다
- 게이트를 통과하지 못하면 올리는 단계까지 가지 않습니다
- 열쇠 만드는 법은 `PUBLISH_AUTO.md` · `YOUTUBE_SETUP.md`

**링크를 어디 두나** — 플랫폼마다 반대입니다.

| | 링크 위치 | 이유 |
|---|---|---|
| 쓰레드 | **답글에** | 첫 게시물에 외부 링크가 있으면 노출이 줄어듭니다 |
| 유튜브 | **설명란에** | 불이익 없음. 블로그를 맨 위에 둡니다 |

### 인스타 · 쓰레드

- **캐러셀 기본** — 단일 이미지 대비 저장 **9배**. 카드는 `flow` 순서 그대로
- **해시태그 5개** (2025.12.18 인스타 공식 상한) — `build_publish.py` 가 생성
- CTA 는 저장보다 **전송** — 공식 랭킹 신호는 시청시간 / 좋아요 / **전송(sends)**
- 캡션은 **첫 두 줄**이 '더 보기' 앞에 보이는 전부입니다
- 표지 공식: `[연도/대상] + [손실 트리거] + [숫자] + 총정리`
  - ⚠️ `99%가 모르는`은 실사용 빈도가 높지만 **우리 톤 규칙에서 금지**
- **쓰레드로 제목 A/B 테스트** — 중앙값이 인스타보다 높고(542 vs 322) 제작 3~10분

### 자동 업로드 — 실측 (2026-08-24)

**"전부 자동"은 불가능합니다.** 주력 채널인 블로그 두 곳이 원천 차단입니다.

| 채널 | API 발행 | 근거 |
|---|---|---|
| 티스토리 | **불가** | Open API 2024년 2월까지 순차 종료. 글쓰기 포함 |
| 네이버 블로그 | **불가** | 개인 블로그 글쓰기 공개 API 없음 |
| 유튜브 | 조건부 | 미인증 프로젝트 업로드분은 **비공개로 잠김**. 해제하려면 audit |
| 틱톡 | 조건부 | 미감사 클라이언트 게시물은 **비공개로 잠김** |
| 인스타·쓰레드 | 가능 | 비즈니스/크리에이터 계정 + Meta 앱 심사 |

**따라서 발행은 브라우저로, 손으로 합니다.** 절차는 `PUBLISH.md`.
원칙은 **임시저장까지만 자동, 발행 버튼은 사람** — 네이버 자동화 탐지 때문입니다.
자동화는 **채널이 살아난 뒤**(글 15~25편, 색인 완료) 다시 판단합니다.

### 이 세션(Cowork)에서 되는 것과 안 되는 것

| | 여기서 | GitHub Actions 에서 |
|---|---|---|
| 이미지·모션 렌더 | **가능** (Playwright+ffmpeg 로컬) | 가능 |
| AI 음성(TTS) | **불가** — speech.platform.bing.com 프록시 차단(SSL) | 가능 |
| 업로드 | 불가 | 가능 |
| git push | 불가 | — |

**만드는 것은 여기, 음성·발행은 GitHub Actions.**
그래서 `git push` 가 병목입니다.


## 12. 파이프라인

```
machimaza/
├── CLAUDE.md              # 이 파일
├── rates.json             # ★ 모든 숫자의 유일한 출처
├── calendar.json          # 역산 캘린더
├── channels.json          # 채널 핸들·주소의 유일한 출처 (인스타·쓰레드만 machi_maza)
├── brand/tokens.css       # 색·폰트 기준값 (렌더러가 같은 값을 갖고 있는지 test_brand.py 가 대조)
├── scripts/
│   ├── pipeline.py        # 단일 파일 렌더러 + 게이트
│   ├── verify.py          # 검수 게이트
│   ├── check_numbers.py   # 수치 정합성 (연도 오염·유령 수치·폐지 제도)
│   ├── test_check_numbers.py  # 위 검사기의 회귀 테스트 (경쟁자 실제 오류 재현)
│   ├── make_images.py     # pipeline.py 로 위임하는 진입점
│   ├── lint_tone.py       # 톤 + 법적 금칙
│   └── audit_stale.py     # 노후화 스캔
├── assets/bgm/            # 배경음·알림음 (출처.md 에 라이선스)
├── docs/                  # GitHub Pages — 홈페이지·개인정보처리방침
│                          #   OAuth 프로덕션 게시에 URL 두 개가 필요했습니다
├── requirements.txt       # 패키지 목록의 유일한 출처
├── .github/workflows/
│   ├── verify.yml         # content/ 변경 시 게이트
│   ├── narrate.yml        # 음성 샘플 · 음성+영상 생성
│   ├── publish.yml        # 발행 (쓰레드·유튜브)
│   └── stale-audit.yml    # 주간 갱신 대상 이슈
└── content/YYYY-MM-DD-주제명/
```

### 작업 순서

| 단계 | 명령 / 방식 |
|---|---|
| 0. 폴더 생성 | `python3 scripts/new_content.py <슬러그>` — 템플릿이 필수 필드를 들고 시작 |
| 1. 주제 선정 | 👤 역산 캘린더 + 검색 의도 |
| 2. 리서치 | 👤 tier 1~2 원문에서 **직접 확인한 수치만** |
| 3. `data.json` | 👤 `items[].wage` 먼저 → 금액은 산식으로. `flow.scenes` 는 **자격→금액→기한→신청→할 일** |
| 4. `blog.md` | 🤖 초안 → 👤 편집 (AI 비중 50% 미만) |
| 5. 검사 3종 | `check_numbers.py` · `lint_terms.py` · `verify.py` |
| 6. 생성 | `pipeline.py` (이미지+영상) · `build_narration.py` · `build_publish.py` |
| 7. 음성·영상 | Actions → **음성·영상** → `build`. 나레이션·배경음·카드·영상이 한 번에 |
| 8. 발행 | Actions → **발행**. **기본은 미리보기**, `publish: true` 여야 올라갑니다 |

**이 컨테이너에서는 TTS 가 프록시에 막혀 있습니다.** 러너에서 돌립니다.
`--dry` 를 쓰면 음성 없이 배치만 검증할 수 있습니다.

**스크립트 지도**

| 파일 | 하는 일 |
|---|---|
| `rates.json` | 숫자의 유일한 출처 |
| `terms.json` | 용어 표기의 유일한 출처 |
| `content/_template/` | 새 글의 출발점 |
| `check_numbers.py` | 연도 오염·유령 비율·산식 불일치·폐지 제도 |
| `lint_terms.py` | 등록 용어 첫 등장에 뜻풀이가 있는가 |
| `lint_tone.py` | 과장·의료법·금소법 금칙 |
| `verify.py` | 위 셋 + 출처·축·flow 구조·영상 규격 |
| `motion.py` | 영상 (t→프레임) |
| `pipeline.py` | 포스터 + 카드 (flow 기준) |
| `build_narration.py` | 대본 검사 |
| `build_publish.py` | 티스토리·네이버·인스타·쓰레드 발행 키트 |
| `tts.py` | 음성 합성 창구 — `edge`(무료 3종) / `azure`(한국어 10종) |
| `build_voice.py` | 장면 시각에 맞춰 나레이션 배치 + 배경음·알림음 얹기 |
| `bgm.py` | 배경음 길이 맞추기 · 크기 고르기 · 대사 아래로 물러나기(더킹) |
| `voice_samples.py` | 목소리·빠르기·높낮이 후보를 합본으로 만들어 고르게 |
| `release_asset.py` | 영상에 공개 주소 붙이기 (쓰레드·인스타가 가져갈 자리) |
| `publish_threads.py` | 쓰레드 발행 — 링크는 답글로 |
| `publish_youtube.py` | 유튜브 쇼츠 업로드 |
| `yt_token.py` | 👤 본인 PC 에서 유튜브 갱신 토큰 받기 (한 번만) |
| `check_remote.py` | 원격이 로컬과 **글자까지** 같은지 blob 해시로 대조 |
| `test_tts.py` | 빈 시크릿·SSML 회귀 테스트 |
| `test_brand.py` | 렌더러 색이 `tokens.css` 와 같은지 · 핸들이 `channels.json` 밖에 적혔는지 |
| `channels.json` | **채널 핸들·주소의 유일한 출처** |
| `build_glossary.py` | 용어 사전 페이지 |
| `test_*.py` | 검사기가 살아 있는지 확인 (CI 가 콘텐츠보다 먼저 실행) |
| `requirements.txt` | **패키지 목록의 유일한 출처** — 워크플로 셋이 이것만 봅니다 |

**열쇠는 전부 GitHub Secrets 에 둡니다. 저장소가 공개라 더더욱.**

| Secret | 쓰는 곳 | 만드는 법 |
|---|---|---|
| `AZURE_SPEECH_KEY` · `AZURE_SPEECH_REGION` | 나레이션 | `VOICE_SETUP.md` |
| `YT_CLIENT_ID` · `YT_CLIENT_SECRET` · `YT_REFRESH_TOKEN` | 유튜브 | `YOUTUBE_SETUP.md` |
| `THREADS_USER_ID` · `THREADS_ACCESS_TOKEN` | 쓰레드 | `PUBLISH_AUTO.md` |

세 개는 **같은 클라이언트에서 나와야** 합니다. 섞이면 `unauthorized_client` 입니다.

### 검수 게이트

| 항목 | 판정 |
|---|---|
| 본문 모든 숫자가 `rates.json`과 일치하는가 | 🤖 |
| 모든 항목에 tier 1~2 출처가 연결됐는가 | 🤖 |
| 출처 URL이 `.go.kr`/`.or.kr`/`.re.kr` 인가 | 🤖 |
| 근거 연도·최종 확인일이 표기됐는가 | 🤖 |
| 본문 8,000자 이상 / H2 7~9개 / FAQ 5+ | 🤖 |
| 이미지 5장 이상 | 🤖 |
| 텍스트 표 1개 이상 | 🤖 |
| 저자 서명·고지 문구 | 🤖 |
| 법적 금칙 (질병+효능, 의료광고, 금융상품) | 🤖 |
| 레이아웃이 직전 3건과 다른가 | 🤖 |
| **사실 자체가 맞는가** | 👤 **사람만 가능** |

**🔴이면 발행하지 않습니다.** 기준을 낮추거나 코드를 고쳐 통과시키지 않습니다.

### 교차검증 규칙

> 금액·기준·날짜는 **서로 다른 tier 1~2 출처 2곳**에서 일치할 때만 단정합니다.
> 한 곳에서만 확인되면 "○○ 기준"으로 출처를 명시하고 단정하지 않습니다.
> 확인이 안 되면 그 항목을 빼고, 항목이 3개 미만이면 그 건을 폐기합니다.

---

## 13. 지표와 되돌림

### 초기 3개월 — 조회수를 보지 않습니다

첫 3개월에 조회수를 보면 반드시 실망하고, 실망하면 그만둡니다.
신규 도메인의 1년 내 top10 진입률은 **1.74%**이고, 네이버 신규 블로그는 **3~6개월** 누적 후에야 반응합니다.

| 지표 | 목표 |
|---|---|
| 게이트 1회 통과율 | 60% → 90% |
| 1건 제작 소요 시간 | 4h → 2h |
| `rates.json` 참조율 | 100% (직접 타이핑 0) |
| 갱신 지연 | 개정 후 7일 이내 반영 |

### 4개월 차부터

**KPI는 "최근 30편 중앙 조회수" 하나**입니다. 구독자·팔로워는 보지 않습니다.

> 실측 반례: 구독 50만 채널의 최근 중앙값 232회 / 구독 669명 채널의 최고 5.6만
> 인스타 16.6만 팔로워 계정의 평균 좋아요 54

### 되돌림 트리거

| 신호 | 조치 |
|---|---|
| 티스토리 등급 `bad` | 블로그 주 1건으로 축소 |
| 신규 글 색인 지연 7일↑ | 발행 중단, 기존 글 품질 보강 |
| 네이버 유입 2주 연속 -50% | 네이버 발행 중단 |
| 30편 중앙 조회수 1,000 미만 | **소재군 교체 검토** |
| 애드센스 3회 거절 | 콘텐츠 밀도 재설계 |

**주 1회 점검일: 월요일.** 티스토리 등급(`tistory.com/api/v1/init`의 `level`),
색인 상태(`site:` 검색), 갱신 대상 이슈를 확인합니다.

---

## 14. 검증 기록 — 옛 방식으로 돌아가지 마세요

14건의 검증에서 나온 결론입니다. **각 항목은 실측 데이터에 근거합니다.**

### 하지 말 것 (실패가 확인됨)

| # | 결론 | 근거 |
|---|---|---|
| 1 | **무인 대량 자동 발행** | 2,000편 → 클릭 39건 / 2,300편 16개월 → 트래픽 -90%, 노출 페이지의 94%가 클릭 0 |
| 2 | **3개 카테고리 병렬 시작** | 병렬로 성공한 사례 0건. 주제권위 상위는 첫 클릭 8.2배 빠름 |
| 3 | **롱테일 대량 양산** | AI Overview가 붙는 키워드의 60%가 월 검색량 100회 이하 |
| 4 | **모션 없는 슬라이드쇼 + AI 음성** | 틱톡 unoriginal 플래그 명시 대상 / 유튜브 2026.1 상위 AI 채널 16개 조치. AI 음성을 쓸수록 §9의 5개 조건이 더 중요해짐 |
| 5 | **"○○원 지급" 프레임** | 지급 안내형 9,900회 vs 박탈·처벌형 9.5만~11만회 |
| 6 | **법제처 API 직접 호출 (클라우드)** | 클라우드 IP에 안티봇 JS 반환. 프로덕션 파이프라인은 전부 self-hosted |

### 할 것 (근거가 확인됨)

| # | 결론 | 근거 |
|---|---|---|
| 7 | **갱신 정확도를 차별점으로** | 상위 계산기·글 전부가 2026년 수치를 틀림. 공단 공식 페이지도 2년 방치 |
| 8 | **1개 니치 집중** | 첫 클릭 5.2일 vs 12.1일 |
| 9 | **본문 8,000~9,000자** | 상위 글 실측 범위 1,800~20,000, 중앙 8,000~9,000 |
| 10 | **제외·탈락 사유 섹션** | 상위 글의 차별점. 하위 글엔 없음 |
| 11 | **AI 비중 50% 미만** | 상위 1~3위의 82.2%가 이 구간. 무편집 2,000편은 CTR 0.13% |
| 12 | **브랜드 = AI 시대의 트래픽** | 브랜드 멘션 상관 0.664 vs 백링크 0.218 |
| 13 | **손실 트리거 프레임** | "모르면 손해" 계열이 지원금 콘텐츠에서 최다 출현 |
| 14 | **캐러셀 · 해시태그 5개 · 전송 CTA** | 캐러셀 저장 9배 / 인스타 2025.12 공식 상한 / 공식 랭킹 신호 |

### 조용히 지나가던 것들 (2026-08-25)

**오류를 내지 않고 넘어가던** 버그들입니다. 각각에 검사를 붙였습니다.
검사가 없으면 같은 자리에서 또 당합니다.

| 무엇 | 어떻게 조용했나 | 지금 무엇이 잡나 |
|---|---|---|
| 커넥터가 글자를 바꿈 | `"장면보다 김"` → `"장면보다 길"`. 오류 없음 | `check_remote.py` — blob 해시 대조 |
| 옛 산출물이 최신인 척 | 영상이 옛 흐름 그대로 32.5초. 게이트는 그걸 잼 | `.render.json` — data.json 해시 도장 |
| 나레이션이 첫 대사에서 잘림 | `amix duration=first` 가 **필터 첫 입력**을 봄. 무음 바닥을 뒤에 둬서 4초에서 끊김 | 만든 뒤 길이 대조, 어긋나면 즉시 중단 |
| 영상이 통째로 잘림 | `-shortest` 가 짧은 나레이션에 맞춰 자름 | 완성 영상 길이 대조 |
| 빈 시크릿이 주소를 망가뜨림 | 러너는 없는 시크릿을 **빈 문자열**로 넘김. `os.environ.get(k, 기본값)` 은 그때 기본값을 안 씀 | `test_tts.py` — `or` 로 받기 |
| 워크플로마다 패키지 목록이 다름 | `publish.yml` 에만 `markdown` 이 없어 발행이 멈춤 | `requirements.txt` 하나 + `test_workflow.py` 검사 |
| 여백이 비대칭 | 왼쪽 100 / 오른쪽 190 이라 글이 왼쪽으로 밀림 | 9장에 안전영역 근거를 기록 |
| `set -e` 에서 `[ ] && ...` | 조건이 거짓이면 그 줄이 실패로 잡혀 스크립트가 죽음 | `if` 로 쓰기 (`test_workflow.py` 가 bash 문법 검사) |
| 본문이 상자를 넘쳐 글자가 겹침 | 표 밑 메모가 세 줄이 되자 출처줄·채널명 위로 내려앉음. 브라우저는 넘친 글도 그려줌 | `motion.py` `FIT_JS` — 장면마다 상자를 재고 침범하면 중단 |
| 렌더러 색이 토큰과 어긋남 | `brand/tokens.css` 는 '단일 소스'라 적혀 있었지만 아무도 읽지 않음 | `test_brand.py` — 토큰에 없는 색이면 CI 중단 |

**교훈 하나로 줄이면** — 만든 것이 설계대로인지 **만든 직후에 재는** 습관입니다.
길이·해시·개수는 재기 쉽고, 재지 않으면 반드시 어긋난 채 흘러갑니다.

### 냉정한 기대치

| 시점 | 낙관 (10~15%) | 비관 (50~60%) |
|---|---|---|
| 6개월 | 2~5만원 | 2천~7천원 |
| 12개월 | 10~25만원 | 1~3만원 |
| 24개월 | 35~70만원 | 2~6만원 |

**12개월차 기대 월수익 약 8만원.** 하방은 얕습니다 — 현금 노출 연 10만원 이하.
잃는 건 시간이고, 그 시간에 배우는 건 남습니다.

---

## 15. 현재 상태

기준일 **2026-08-25**. 이 절은 세션이 바뀌어도 "어디까지 왔는지"를 알려주는 자리입니다.
새 사실이 생기면 여기부터 고치세요.

### 완결된 축 — 다시 논의하지 않습니다

| 축 | 상태 | 잠긴 곳 |
|---|---|---|
| 니치·정체성 | "마침 필요한 정보를, 알맞게" | §1·§2 |
| 원장 4개 | `rates`·`terms`·`style`·`categories` | §4 |
| 검사기 + 회귀 테스트 | 게이트 6종 · 테스트 5종 | §12 |
| **음성** | Azure `ko-KR-HyunsuMultilingualNeural` · `+15%` / `+10Hz` | **§6 BRAND.md** |
| **배경음·알림음** | 사용자가 고른 파일 두 개 (`assets/bgm/`) | §10 · `BGM.md` |
| **영상 레이아웃** | 1080×1920 · 여백 210/190/720/190 · 본문 가운데 | §9 |
| 저장소 | `github.com/machimaza/desk` 공개 · CI 초록불 | 아래 |
| 채널 개설 | 유튜브·틱톡·인스타·쓰레드·티스토리·네이버 `machimaza` (쓰레드만 `machi_maza`) | §11 |
| **유튜브 자동 업로드** | OAuth 프로덕션 게시 완료 · 실제 업로드 성공 | `YOUTUBE_SETUP.md` |

유튜브에 올라간 것 (둘 다 **비공개**)

| 영상 | 상태 |
|---|---|
| `watch?v=b7py0GAG-Ok` | **현재 판.** 워터마크 왼쪽 위 모서리 · 46.8초 |
| `watch?v=7K1HI6HKkeo` | 채널명이 맨 위 가운데. **지우세요** |
| `watch?v=MvYsmq5SBc8` | 채널명이 칩 사이에 묻힘. **지우세요** |
| `watch?v=NURoIFxwctg` | 표 밑 메모가 출처줄과 겹침. **지우세요** |
| `watch?v=fNLhj4mZAYQ` | 옛 레이아웃(왼쪽 치우침). **지우세요** |

고칠 때마다 새 영상이 하나씩 생깁니다. **공개 전환은 맨 마지막에 한 번만** 하세요.

**유튜브는 영상 파일을 바꿔치기할 수 없습니다.** 고칠 때마다 새로 올리고 옛 것을 지웁니다.
그래서 공개 전환은 항상 **마지막**에 합니다.

### 남은 일 (순서대로)

- [ ] **환급금 글 발행** — 8월 파도. 시간에 민감합니다
- [ ] **퇴직후 건강보험료 편 업로드**
- [ ] **쓰레드 열쇠 두 개** (`THREADS_USER_ID`·`THREADS_ACCESS_TOKEN`) → 첫 자동 발행
- [ ] **틱톡 업로더** — 구조는 유튜브와 같습니다 (`publish_youtube.py` 를 본뜨면 됩니다)
- [ ] 블로그 발행 (티스토리·네이버) — **손으로 합니다.** 이유는 §11
- [ ] 경감고시 원문 대조 (`rates.json` `_검증필요`)
- [ ] 애드센스 신청 (글 15~25편, 색인 완료 후)

### 저장소를 다루는 법 (컨테이너에서)

읽기는 `git clone` / `git fetch` 로 됩니다. **쓰기는 Composio GitHub 커넥터**입니다.
`api.github.com` 은 403 이고, `raw.githubusercontent.com` 은 **CDN 캐시라 옛 내용이 옵니다** —
최신 내용을 읽어야 하면 `GITHUB_GET_REPOSITORY_CONTENT`(base64) 를 쓰세요.

커넥터는 REST API 라서 **파일 전체를 통째로 덮어씁니다.** 손으로 옮겨 적으면 글자가 바뀝니다 —
실제로 `build_voice.py` 의 "장면보다 김" 이 "장면보다 길" 로 바뀐 적이 있습니다.
그래서 규칙: **올리기 전에 blob 해시를 대조합니다.**
`git hash-object <파일>` 값과 원격 트리의 `sha` 가 같아야 올린 것이 원본과 같습니다.
올린 뒤에는 `python3 scripts/check_remote.py` 로 전체를 한 번 더 대조하세요.

**바이너리는 올리지 마세요.** base64 가 문맥을 통째로 먹습니다.
받아오는 쪽은 임시 브랜치 + `git archive` 로 공짜입니다. 임시 브랜치는 쓰고 나서 지웁니다.

### 지운 것들 (2026-08-25) — 되살리지 마세요

| 지운 것 | 이유 |
|---|---|
| `scripts/_legacy/` | v2 렌더러. `pipeline.py` 와 결과가 달라 "어느 쪽으로 뽑았나"를 흐립니다 |
| `templates/` | `_legacy/render.py` 만 읽던 파일들 |
| `schema/data.schema.json` | `flow` 를 모릅니다. 진짜 계약은 `verify.py` 입니다 — 두 번째 진실은 어긋납니다 |
| `content/_pipeline-test/` | 옛 렌더 찌꺼기. 게이트가 이미 제외하고 있었습니다 |
| `CLAUDE.v2.archive.md` | git 이력에 있습니다 |
| `PUSH_GUIDE.md` · `GIT_SETUP.md` | 저장소가 이미 올라갔습니다. 폐기된 토큰 안내가 남아 있었습니다 |
| `bgm.py` 의 합성 코드 | 사용자가 합성음을 물렸습니다("알림음도 별로임"). 고른 파일을 씁니다 |
| `pipeline.py` 의 `SCENE_CSS` | 영상 CSS 복사본. 아무도 안 쓰면서 여백만 옛것(150/430)으로 남아 있었습니다 |
| `pipeline.py` 의 `card()` | flow 이전의 카드 함수. 호출하는 곳이 없고 없는 변수를 참조했습니다 |
| 핸들 하드코딩 3곳 | `publish_youtube.py`·`docs/index.html`·`BRAND.md`. `channels.json` 하나로 모았습니다 |

---

## 16. 알려진 제약

- **컨테이너에서 `.go.kr` 직접 접근 차단.** WebFetch는 작동하므로 리서치는 가능하나 API 호출은 불가.
- **컨테이너에서 음성 합성(TTS)도 막혀 있습니다.** 프록시가 Azure·Edge 를 끊습니다.
  나레이션·영상은 **Actions 러너에서만** 만들어집니다. 여기서는 `--dry` 로 배치만 검증하세요.
- **작은창(PIP)의 검은 여백은 고치지 않습니다.** 파일은 1080×1920 가득 찬 것이 맞고,
  9:19.5 화면이 9:16 을 레터박싱하는 것입니다. 이걸 쫓으면 안전영역과 타 플랫폼 재사용이 깨집니다.
- **티스토리·네이버는 자동 발행하지 않습니다.** 두 곳 다 약관이 자동화를 금지하고,
  애드센스가 붙은 블로그의 계정 정지는 되돌릴 수 없습니다. 발행 키트를 만들어 손으로 올립니다.
- **애드센스 승인이 최대 선행 장벽.** 계산기·도구 65개 + 글 15편으로도 거절된 사례가 있습니다.
  개수가 아니라 **페이지당 편집 콘텐츠 밀도**가 기준입니다.
- **네이버 하루 2건은 권장선(1~2건) 상단.** 색인 지연이 보이면 즉시 1건으로.
- **게이트가 못 잡는 것: 사실 자체의 오류.** 교차검증 2출처 규칙이 유일한 방어입니다.
- **재산 정률제 개편은 미확정.** 2026.7 개편안에서 빠졌고 건정심 상정도 취소됐습니다.
  요율·기본공제가 공개되지 않아 관련 콘텐츠는 골격만 준비하고 대기합니다.
- **2027.1 상하한 개편은 확정 임박.** 하한 최저임금 연동은 **지역가입자 하위 50.7%(486만 세대)**
  에 영향을 줍니다. 발표 후 시행까지 1~2개월밖에 없으니 골격을 미리 만들어둡니다.
