# 자동 업로드

## 어디가 되고 어디가 안 되나

| 플랫폼 | 자동 | 사정 |
|---|---|---|
| **쓰레드** | ✅ | 본인 계정은 Meta 앱 검수 없이 됩니다 |
| **유튜브** | 🟡 | 업로드는 되지만 **미검수 앱은 비공개로만** 올라갑니다 (검수 2~4주) |
| **틱톡** | 🟡 | 업로드는 되지만 **심사 전에는 비공개로만** 올라갑니다 |
| **인스타** | 🟡 | 페이스북 페이지 연결 + 앱 검수가 필요합니다 |
| 티스토리 | ❌ | Open API 가 2024년 2월에 종료됐습니다 |
| 네이버 블로그 | ❌ | 글쓰기 API 가 없습니다 |

영상 플랫폼 셋(유튜브·틱톡·인스타)은 모양이 같습니다 —
**올리는 것은 자동, 공개로 돌리는 것은 손으로.** 심사를 통과하면 공개까지 자동이 됩니다.
어차피 발행 전에 사람이 확인할 항목이 있어서, 이 순서가 오히려 안전합니다.

티스토리·네이버를 브라우저 자동화로 미는 방법이 있지만 **쓰지 않습니다.**
두 곳 다 약관에서 자동화를 금지하고, 걸리면 계정이 정지됩니다.
애드센스를 붙일 블로그를 그렇게 잃을 수는 없습니다. 이 둘은 `publish/` 폴더의
붙여넣기 키트로 손으로 올립니다.

---

## 쓰레드 열쇠 만들기

한 번만 하면 됩니다. 토큰은 60일마다 갱신해야 합니다.

### 1. 앱 만들기

<https://developers.facebook.com/apps> → **앱 만들기**

- 유스케이스에서 **Threads API** 를 고릅니다
- 앱 이름은 아무거나 (`machimaza-publish` 정도)

### 2. 권한 붙이기

앱 대시보드 → **Threads API** → **설정**

필요한 권한 두 개를 추가합니다.

- `threads_basic`
- `threads_content_publish`

**검수는 신청하지 않아도 됩니다.** 본인 계정과 테스트 계정에는 개발 모드에서
그대로 올릴 수 있습니다. 남의 계정에 올릴 일이 없으니 검수가 필요 없습니다.

### 3. 계정 연결

**Threads 계정 추가** 에서 `@machi_maza` 를 연결합니다.

### 4. 토큰 받기

**액세스 토큰 생성** 을 누르면 단기 토큰이 나옵니다. 이건 한 시간이면 만료되므로
**장기 토큰으로 바꿔야** 합니다. 앱 대시보드에 바꾸는 버튼이 있고,
없으면 아래 주소를 브라우저에 넣어도 됩니다.

```
https://graph.threads.net/access_token
  ?grant_type=th_exchange_token
  &client_secret=<앱 시크릿>
  &access_token=<단기 토큰>
```

돌아온 `access_token` 이 60일짜리입니다.

### 5. 사용자 번호 확인

```
https://graph.threads.net/v1.0/me?fields=id,username&access_token=<장기 토큰>
```

`id` 값이 사용자 번호입니다.

### 6. Secrets 에 넣기

<https://github.com/machimaza/desk/settings/secrets/actions>

| Name | 값 |
|---|---|
| `THREADS_USER_ID` | 5단계의 `id` |
| `THREADS_ACCESS_TOKEN` | 4단계의 장기 토큰 |

---

## 올리기

Actions → **발행** → Run workflow

| 칸 | 설명 |
|---|---|
| `content` | 글 폴더 |
| `target` | `threads` |
| `link` | 답글에 달 블로그 주소 (비우면 답글 없음) |
| `publish` | **false 면 미리보기**, true 여야 실제로 올라갑니다 |

**기본은 미리보기입니다.** 한 번 돌려서 본문과 영상 주소를 눈으로 확인하고,
그 다음에 `publish` 를 켜서 다시 돌리는 순서를 권합니다.

검수 게이트를 통과하지 못하면 올리는 단계까지 가지 않습니다.

### 왜 링크를 답글에 다나

첫 게시물에 외부 링크가 있으면 노출이 줄어듭니다. 본문은 링크 없이 두고,
올라간 뒤 답글로 링크를 답니다. 스크립트가 알아서 합니다.

### 영상은 어떻게 넘어가나

쓰레드는 파일을 직접 받지 않고 **주소를 주면 가져갑니다.**
그래서 영상을 릴리스 첨부파일로 올려 공개 주소를 만든 뒤 그 주소를 넘깁니다.
릴리스는 덮어쓸 수 있어서 저장소 기록이 무거워지지 않습니다.

---

## 토큰이 만료되면

60일이 지나면 `쓰레드 응답 400 … error 190` 같은 것이 뜹니다.
4단계를 다시 해서 새 토큰을 Secrets 에 덮어쓰면 됩니다.

만료 전에 갱신하면 같은 주소로 연장됩니다. 달력에 55일 뒤 알림을 걸어두시면 편합니다.
